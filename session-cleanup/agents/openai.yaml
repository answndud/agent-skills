interface:
  display_name: "Session Cleanup"
  short_description: "Preview and prune stale Codex sessions"
  default_prompt: "Use $session-cleanup to review stale Codex sessions and keep live or named chats safe."
policy:
  allow_implicit_invocation: true
